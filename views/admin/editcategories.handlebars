{{#each errors}}
<div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}

<h3>Editar Categoria</h3>
<div class="card">
<div class="card-body">
<form action="edit" method="POST" >
<input type="hidden" name="id" value="{{category._id}}">
<label for="name" >Nome</label>
<input type="text" id="name" placeholder="Nome da cateoria" name="name" class="form-control" value="{{category.name}}">
<label for="slug" id="slug" name="slug" >Slug</label>
<input type="text" name="slug" id="slug" placeholder="Slug da categoria" class="form-control" value="{{category.slug}}">
<br>
<button  type="submit" class="btn btn-success ">Editar</button>



</form>
</div>



</div>